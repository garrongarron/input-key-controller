import Loader from "./Loader.js";

let animations = [
    'caminar-adelante',
    'caminar-atras',
    'correr-adelante',
    'correr-atras'
]

let warrior = new Loader('warrior', animations)

export default warrior